diff --git a/src/video/x11/SDL_x11video.c b/src/video/x11/SDL_x11video.c
index 1ca7e021d..bec01cdf2 100644
--- a/src/video/x11/SDL_x11video.c
+++ b/src/video/x11/SDL_x11video.c
@@ -431,7 +431,7 @@ static bool X11_VideoInit(SDL_VideoDevice *_this)
     GET_ATOM(XKLAVIER_STATE);
 
     // Detect the window manager
-    X11_CheckWindowManager(_this);
+    // The embedded Bachata X server provides the Android-managed window and no EWMH window manager.
 
     if (!X11_InitModes(_this)) {
         return false;
@@ -447,7 +447,7 @@ static bool X11_VideoInit(SDL_VideoDevice *_this)
     X11_InitXfixes(_this);
 #endif
 
-    X11_InitXsettings(_this);
+    // The embedded Bachata X server does not expose an XSETTINGS manager.
 
 #ifdef SDL_VIDEO_DRIVER_X11_XSYNC
     X11_InitXsync(_this);
